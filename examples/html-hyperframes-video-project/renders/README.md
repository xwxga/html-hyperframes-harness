# Render Notes

The full final MP4 is intentionally not committed to this public package because it is large. This folder includes `html_hyperframes_5frame_preview.mp4`, a short low-resolution preview derived from the final render for lightweight review.

Use the preview MP4 or `../snapshots/` for quick visual inspection. The render source of truth is `../index.html`.
